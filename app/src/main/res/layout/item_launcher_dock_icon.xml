<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="item"
            type="com.bg.global.ioslauncher.ui.launcher.workspace.LauncherIconUiModel" />
    </data>

    <FrameLayout
        android:id="@+id/dockIconRoot"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:focusable="false"
        android:padding="8dp">

        <FrameLayout
            android:id="@+id/iconPlate"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:clickable="true"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:elevation="2dp"
            android:focusable="true"
            android:padding="0dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@{item.label}"
                android:scaleType="fitCenter"
                android:src="@{item.icon}" />

            <FrameLayout
                android:id="@+id/removeBadge"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="top|start"
                android:background="@drawable/bg_edit_remove_badge"
                android:clickable="true"
                android:elevation="8dp"
                android:focusable="true"
                android:translationX="-8dp"
                android:translationY="-8dp"
                android:visibility="gone">

                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_minus_18" />
            </FrameLayout>
        </FrameLayout>
    </FrameLayout>
</layout>
